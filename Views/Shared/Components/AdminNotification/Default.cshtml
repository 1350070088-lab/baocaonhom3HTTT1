@model int

@if (Model > 0)
{
    <span class="absolute -top-1 -right-1 bg-rose-500 text-white text-[10px] font-black w-4 h-4 flex items-center justify-center rounded-full shadow-lg animate-pulse">
        @Model
    </span>
}
